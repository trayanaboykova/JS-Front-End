/*
 * index.js: Top-level plugin exposing HTTP features in flatiron
 *
 * (C) 2011, Charlie Robbins & the Contributors
 * MIT LICENSE
 *
 */

var union = exports;

//
// Expose version information
//
exports.version = require('union/package.json').version;

//
// Expose core union components
//
union.BufferedStream = require('union/lib/buffered-stream');
union.HttpStream     = require('union/lib/http-stream');
union.ResponseStream = require('union/lib/response-stream');
union.RoutingStream  = require('union/lib/routing-stream');
union.createServer   = require('union/lib/core').createServer;
union.errorHandler   = require('union/lib/core').errorHandler;
