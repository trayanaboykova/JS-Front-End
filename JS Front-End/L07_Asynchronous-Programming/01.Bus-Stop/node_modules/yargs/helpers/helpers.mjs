import {applyExtends as _applyExtends} from 'yargs/build/lib/utils/apply-extends.js';
import {hideBin} from 'yargs/build/lib/utils/process-argv.js';
import Parser from 'yargs-parser';
import shim from 'yargs/lib/platform-shims/esm.mjs';

const applyExtends = (config, cwd, mergeExtends) => {
  return _applyExtends(config, cwd, mergeExtends, shim);
};

export {applyExtends, hideBin, Parser};
